name: weather_app
description: A weather application with day length calculation

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8  
  http: ^1.1.0
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.18.1
  camera: ^0.10.5
  url_launcher: ^6.1.11
  geolocator: ^9.0.2
  flutter_svg: ^2.0.7  
  material_design_icons_flutter: ^7.0.7296  

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  build_runner: ^2.4.4

flutter:
  uses-material-design: true  
  

  

  
